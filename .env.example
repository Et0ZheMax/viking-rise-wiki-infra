# Пример значений для docker-compose.yml.
# Задай свои значения в .env перед запуском.

POSTGRES_USER=wikijs
POSTGRES_PASSWORD=change_me_strong_password
POSTGRES_DB=wikijs

# Порт, который будет опубликован Nginx на хост-машине.
# Например, 8080 -> приложение доступно на http://localhost:8080
PUBLIC_HTTP_PORT=8080
